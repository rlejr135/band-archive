apiVersion: v1
kind: ConfigMap
metadata:
  name: band-archive-config
  namespace: band-archive
data:
  FLASK_CONFIG: "config.ProductionConfig"
  DATABASE_URL: "sqlite:////data/band_archive.db"
  CORS_ALLOWED_ORIGINS: "http://band.yourdomain.com"
  VITE_API_URL: "http://api.yourdomain.com"
